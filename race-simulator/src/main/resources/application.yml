spring:
  application:
    name: race-simulator

tracking-app:
  url: http://localhost:8080

cron:
  expression: "* 37 * * * *"

logging:
  level:
    org.springframework.web.reactive: INFO
---
spring:
  profiles: dev
cron:
  expression: "*/5 * * * * * "
